[project]
name = "cursor-clone"
version = "0.1.0"
description = "A CLI coding agent powered by Claude and LangGraph"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.100.0",
    "langgraph>=1.1.10",
    "python-dotenv>=1.2.2",
]
